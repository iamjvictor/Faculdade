//Trabalho de translação de vetores
  #include <stdio.h>
  #include <stdlib.h>

int main (){
    int n, i,vx[5] , vy[5], cx, cy;
   
   

        for (i=0; i<5; i++){
            printf("Digite x");
            scanf("%d", &vx[i]);
            printf("Digite y");
            scanf("%d", &vy[i]);
        }
        
        for (i=0; i<5; i++){
            printf("%d, %d \n", vx[i], vy[i]);
            
            
        }

        //primeira parte
return (0);
}